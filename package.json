{
  "name": "github-webhook-types",
  "version": "0.0.0",
  "homepage": "https://github.com/snomiao/github-webhook-types#readme",
  "license": "MIT",
  "author": "snomiao <snomiao@gmail.com>",
  "files": [
    "*.ts",
    "dist"
  ],
  "repository": "https://github.com/snomiao/github-webhook-types",
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist",
    "prerelease": "bun run build && bun run test",
    "release": "bunx standard-version && git push --follow-tags && npm publish",
    "test": "bun test",
    "prepare": "husky"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "type": "module",
  "exports": {
    "import": "./dist/index.js",
    "types": "./src/index.ts"
  },
  "module": "index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "octokit": "^5.0.3"
  },
  "devDependencies": {
    "@types/bun": "^1.2.20",
    "@types/jest": "^30.0.0",
    "expect-type": "^1.2.2",
    "husky": "^9.1.7",
    "semantic-release": "^24.2.7"
  }
}
